<?xml version="1.0"?>
<ruleset name="WordPress Custom Ruleset"
         xmlns="http://pmd.sf.net/ruleset/1.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0 
                             http://pmd.sf.net/ruleset_xml_schema.xsd"
         xsi:noNamespaceSchemaLocation="http://pmd.sf.net/ruleset_xml_schema.xsd">

    <description>
        Custom ruleset for WordPress and Understrap coding standards.
    </description>

    <!-- Built-in rules -->
    <rule ref="rulesets/cleancode.xml"/>
    <rule ref="rulesets/codesize.xml"/>
    <rule ref="rulesets/design.xml"/>
    <rule ref="rulesets/naming.xml"/>
    <rule ref="rulesets/unusedcode.xml"/>

    <!-- WordPress specific custom rules -->
    <rule name="AvoidWordPressDirectDBAccess"
          class="net.sourceforge.pmd.lang.rule.AbstractJavaRule">
        <description>
            Avoid direct database access. Use WordPress functions for database interactions.
        </description>
        <priority>2</priority>
        <example>
            <![CDATA[
            // BAD
            $wpdb->query("SELECT * FROM $wpdb->posts");

            // GOOD
            get_posts();
            ]]>
        </example>
    </rule>
    
    <!-- Add more WordPress-specific rules as needed -->
</ruleset>
